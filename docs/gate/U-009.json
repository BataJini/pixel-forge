{
  "unit": "U-009",
  "title": "Export: PNG (scaled) + SVG",
  "verifiedAt": "2026-07-01",
  "worktree": ".claude/worktrees/wf_023eceaa-423-25",
  "ranCommands": true,
  "buildExit": 0,
  "testExit": 0,
  "artifactsPresent": true,
  "rewardHackFlags": [],
  "evidence": "Verified in builder worktree wf_023eceaa-423-25 (U-009 not yet on master; state=pending). BUILD: `npm run build` (tsc -b && vite build) exit 0, emits dist/index.html + hashed assets. TESTS: `npm test` (vitest run) exit 0 -> 16 files / 172 passed, includes held-out docs/acceptance/U-009/export.acceptance.test.ts (3/3: viewBox 0 0 4 4, crispEdges, transparent omission, merged rect<64). Ran held-out file in isolation --reporter=verbose exit 0. BROWSER: `npm run test:browser` exit 0 -> 5 files / 25 passed incl. export.browser.test.tsx: scaleToCanvas exact scale x scale blocks + no new colors for scales 1,2,4,8; 512x512 @4x = 2048x2048; PNG signature + transparent + matte flatten; CRT full vs off byte-identical (effect-free). typecheck `tsc -b` exit 0. ARTIFACTS present: src/core/exporters/{png,svg,index}.ts, src/platform/exporters/{encode,save,index}.ts, src/ui/export/, e2e/export.spec.ts, dist/index.html; browser-fs-access dep added. REWARD-HACK scan clean: held-out test byte-identical to HEAD (git diff empty), no skipped/xfail/weakened assertions, genuine greedy rect-merge + integer nearest-neighbor impls (not stubbed), tracked diffs additive only (index re-exports, vite.config U-009 include activation is sanctioned by config design, package.json dep). NOTE: Playwright download-interception e2e is QA-agent scope, not part of this objective gate.",
  "commands": [
    "npm run build  -> exit 0",
    "npm test       -> exit 0 (172 passed, incl held-out U-009)",
    "npx vitest run docs/acceptance/U-009/export.acceptance.test.ts -> exit 0 (3 passed)",
    "npm run test:browser -> exit 0 (25 passed, incl PNG nearest-neighbor + effect-free)",
    "npm run typecheck -> exit 0"
  ]
}
