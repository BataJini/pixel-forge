# QA Report — U-010 Export: GIF + spritesheet (+JSON atlas)

- **Unit:** U-010 — Export: GIF + spritesheet (+JSON atlas) (spec §3.8, §5; deps U-008 frames, U-009 export)
- **Result:** **PASS** (2 informational/observational findings, none blocking U-010)
- **Date:** 2026-07-02
- **Build under test:** integrated on the repo working tree at
  `C:/Users/bata/Projects/pixel-forge` (HEAD `9f2f6f5` "feat: integrate U-010 export
  GIF + spritesheet (+JSON atlas)"). Everything below was **executed for real**;
  exit codes and outputs are observed, not inferred.
- **Environment:** Windows 11, Node v24.14.1, npm 11.11.0, Vitest 4.1.9, Playwright
  1.61.1 (Chromium 1228 / Chrome 149 headed via MCP), Biome 2.5.2, Vite 7.3.6,
  gifenc 1.0.3, browser-fs-access 0.38.0.

## Deliverable surface (what U-010 ships)
- **Pure core** (`src/core/exporters`):
  - `gif.ts` — `encodeGif`, `encodeGifFromFrames`, `parseGifInfo`. Exact global
    palette for ≤256-color art (pixel-perfect, deterministic) with a gifenc median-cut
    fallback above 256 colors. Pure GIF89a decoder walk for verification.
  - `spritesheet.ts` — `packCels`, `packFrames`, `sliceCel`, `scaleMeta`,
    `atlasToJson`, `nextPowerOfTwo`. Grid / horizontal-strip / vertical-strip packing
    with padding + margin + optional power-of-two; clipped source-over blit guarantees
    the non-overlap invariant.
- **Platform glue** (`src/platform/exporters`):
  - `gifWorker.ts` — dedicated module Web Worker running the pure encoder off-thread.
  - `gif.ts` — `encodeGifInWorker` (worker-first, main-thread fallback), `exportGifFile`.
  - `spritesheet.ts` — `buildSpritesheet`, `exportSpritesheetFile` (PNG + companion JSON).
- **UI** (`src/ui/export/ExportDialog.tsx`): GIF + spritesheet controls, wired into
  `CanvasStage.tsx` via the "Export…" toolbar button.

---

## 1. Objective / automated results (all real runs, observed exit codes)

| Check | Command | Result |
|---|---|---|
| Held-out acceptance (U-010) | `vitest run docs/acceptance/U-010/export.acceptance.test.ts` | **4/4 PASS**, exit 0 |
| Full unit suite (regression) | `vitest run` | **47 files / 640 tests PASS**, exit 0 |
| U-010 unit tests | `vitest run src/core/exporters/gif.test.ts spritesheet.test.ts` | **34/34 PASS**, exit 0 |
| Typecheck | `tsc -b` | exit 0 |
| Production build | `tsc -b && vite build` | exit 0 — emits `dist/assets/gifWorker-*.js` (10.56 kB worker chunk) |
| Browser-mode suite (real Chromium) | `vitest run --config vitest.browser.config.ts` | **15 files / 83 tests PASS**, exit 0 |
| Coverage gate (`src/core/**`, 80%) | `vitest run --coverage` | PASS — `gif.ts` 88.09% stmts, `spritesheet.ts` 88.13% stmts; all-files 95.31% |
| E2E (smoke/export/frames) | `playwright test smoke export frames` | **13/13 PASS**, exit 0 |
| Lint — U-010 files only | `biome check <6 U-010 files>` | clean, exit 0 |
| Lint — whole repo | `biome check .` | **8 errors** — all in U-012 shell files + `CanvasStage.tsx`; **0 in U-010** (see Finding 1) |

The held-out suite (`docs/acceptance/U-010/export.acceptance.test.ts`, builder-protected)
covers: `packFrames` one-rect-per-frame / correct size / within-bounds / non-overlapping /
margin / durations; grid + horizontal + vertical atlas dimensions; GIF `GIF89a` header +
`parseGifInfo` frame count + logical size; and deterministic (effect-free) encode. All pass.

## 2. Held-out acceptance criteria — machine-checkable (each verified)

- ✅ `packFrames(frames,{layout,padding,margin})`: `meta.frames.length == n`, `meta.count == n`;
  rects non-overlapping and within atlas; each rect `w/h == frame size`; spacing == `padding`;
  outer `margin` applied; per-frame `duration` carried.
- ✅ Grid / horizontal-strip / vertical-strip produce the expected atlas dimensions for a
  known count/size (e.g. 4×8px → grid 16×16, horiz 32×8, vert 8×32).
- ✅ GIF encode (verified in **both** Node and real Chromium): bytes begin `GIF89a`;
  decode reports image count == frame count and correct logical `w×h`.
- ✅ Effect-free: GIF is a pure function of the composited buffers — byte-identical with the
  CRT display layer ON vs OFF (proven live by toggling `data-crt` full↔off; `identical: true`).
- ✅ 512×512 frame set encodes without freezing (in a worker) — see §3.

## 3. Manual / review criteria (Playwright + real-browser evaluation)

Driven in real Chromium (dev server `http://localhost:5174/pixel-forge/`), importing the
app's Vite-served modules and exercising the exact platform functions the dialog buttons call.
Evidence JSON: `docs/qa/U-010-artifacts/U-010-browser-core.json`.

**Export GIF dialog**
- ✅ Dialog exposes: PNG scale (1×–32×, default 8×), **"GIF loops forever" default checked**
  (loop count default infinite), layout, padding/margin, power-of-two, and the note
  "encode runs off the main thread". Screenshot: `U-010-export-dialog.png`.
- ✅ Uses per-frame durations: encoded GIF `delaysMs = [100,150,200]` matched the frame durations exactly.
- ✅ **Encoding runs in a Web Worker:** instrumented `Worker` construction — a real module
  worker was built at `…/gifWorker.ts?worker_file&type=module`, posted a `done` message
  (`usedWorker: true`).
- ✅ **UI stays responsive / no freeze:** 8 × **512×512** frames encoded in **76 ms** in the
  worker; a concurrent rAF probe recorded **9 paints, max gap 10 ms** during the encode
  (a synchronous main-thread encode would have shown one ~76 ms stall). Valid `GIF89a`,
  8 frames, 512×512, loop 0.
- ✅ **Progress toast:** `onProgress` streamed once per frame (8/8). Real UI click transitioned
  the live status **"Encoding GIF…" → "GIF exported."** and saved `pixelforge.gif` (`GIF89a`).
- ✅ Scale wired through: 8×8 art at scale 4 encoded to a 32×32 GIF.

**Export Spritesheet dialog**
- ✅ Layout (grid/row/column), padding, margin, power-of-two controls present and honored.
- ✅ **Downloads a PNG + companion `.json` atlas** — `exportSpritesheetFile` saved
  `pixelforge.png` **then** `pixelforge.json` in order (valid PNG signature
  `89 50 4E 47 0D 0A 1A 0A`); real UI click showed **"Spritesheet exported."**
  (`U-010-exported-status.png`) and produced both files (png 258 B + json 472 B).
- ✅ JSON atlas maps `frame_0…N → {x,y,w,h,duration}` with a PixelForge `meta` block
  (`app`, `scale`, `fps`, `image`, `frameCount`).
- ✅ **Atlas slices back to the correct frames:** decoded the real atlas PNG via
  `createImageBitmap`, compared every pixel of every frame rect against the source cel —
  `pngMatchesAtlas: true`, `slicesMatchSource: true`, **0 mismatches** (also effect-free:
  no checkerboard/CRT leak, no color drift). `sliceCel` round-trips exactly.

## 4. Regression, integration, security, accessibility

- **Regression:** full 640-test suite + 83 browser-mode tests + 13 e2e all green; the U-009
  export path (PNG/SVG dialog download) still passes. No prior unit broke.
- **Integration:** UI → platform save (`browser-fs-access`) → real `WritableStream` boundary
  produces the correct saved files with correct names/MIME; the GIF worker chunk is split out
  in the production bundle.
- **Smoke:** app boots on its base path; e2e "no uncaught console errors on load" passes. The
  2 console errors seen via MCP (`/meta.json` 404 at origin root; `browserextension.trustpilot.com`)
  originate from the tester's Chrome extensions (MetaMask/Trustpilot), **not** PixelForge.
- **Security:** client-only, no secrets, no network I/O; exporters are pure functions over pixel
  buffers; the worker uses structured-clone/transfer. No injection or authz surface introduced.
- **Accessibility (dialog):** radiogroups/labels/`aria-live` status region present; controls
  keyboard-reachable. (Full a11y pass belongs to U-013; nothing in U-010 regressed it.)

## 5. Findings

**Finding 1 — LOW / out of scope for U-010 (informational).**
Repo-wide `biome check .` fails with 8 errors, but **none are in U-010 files** — they are all
in still-pending **U-012** shell code (`src/ui/shell/CommandPalette.tsx`, `MenuBar.tsx`,
`shell/index.ts`, `e2e/shell.spec.ts`) plus a `CanvasStage.tsx` a11y/format/import-order item.
The six U-010 files lint clean in isolation. This does not block U-010, but the repo `lint`
step (and thus CI) is currently red at the whole-repo level and should be resolved when U-012
lands. *Fix hint:* run `biome check --write .` under U-012; not a U-010 change.

**Finding 2 — INFO / documented scope boundary (not a defect).**
In the live app, `CanvasStage.tsx` currently feeds the Export dialog a **single** frame
(the tool-session preview buffer, `durationMs 100`) via `getFrames`; the code comment and
master-spec state the full multi-frame FrameStack→export wiring is unified in **U-012**.
U-010's actual deliverable — the multi-frame GIF/spritesheet **encoders**, the platform
**save** actions, and a **dialog that accepts multi-frame input** — is complete and correct,
and multi-frame behavior is proven here by direct invocation (3-frame GIF with distinct
durations; 4-frame grid atlas; 8-frame 512×512 worker encode). No U-010 gap; flagged so the
Reviewer/Builder know an end-to-end *animated* GIF from the UI depends on U-012.

**Process note (for the manager, not a code defect):** `state.json` marks U-010 `verified`,
yet `docs/gate/U-010.json`, `docs/reviews/U-010.md`, and (until now) `docs/qa/U-010.md` were
absent. The QA artifact is now written; the gate + review artifacts are still missing.

## 6. Verdict

**PASS.** Every relevant test type — held-out acceptance, unit, browser-mode, e2e, typecheck,
production build, coverage — passes with observed exit code 0, and every manual/Playwright
criterion (worker-encoded GIF with progress toast + loop + durations; spritesheet PNG + JSON
atlas that slices back to the exact frames; effect-free/clean-export; 512×512 non-freeze) is
verified live in a real browser. No defects in U-010 code. The only lint failures belong to
the unrelated, still-pending U-012.

## Evidence (`docs/qa/U-010-artifacts/`)
- `held-out-acceptance.log` — 4/4 held-out acceptance run
- `U-010-browser-core.json` — real-browser multi-frame GIF / effect-free / spritesheet round-trip / layout results
- `U-010-export-dialog.png` — Export dialog (all GIF/spritesheet controls)
- `U-010-exported-status.png` — dialog after a real export ("Spritesheet exported.")
- `devserver.log` — Vite dev server output
